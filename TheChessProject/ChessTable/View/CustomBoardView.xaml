<Window x:Class="ChessTable.View.CustomBoardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChessTable.View"
        mc:Ignorable="d"
        Title="CustomBoardView" Height="300" Width="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="45"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <ComboBox Margin="3">
                <ComboBoxItem Content="White" IsSelected="True"/>
                <ComboBoxItem Content="Black"/>
            </ComboBox>
            <Rectangle Grid.Row="1" Height="28" Width="28" Fill="Black" Stroke="Silver">
            </Rectangle>
            <Rectangle Grid.Row="2" Height="28" Width="28" Fill="Black" Stroke="Silver">
            </Rectangle>
            <Rectangle Grid.Row="3" Height="28" Width="28" Fill="Black" Stroke="Silver">
            </Rectangle>
            <Rectangle Grid.Row="4" Height="28" Width="28" Fill="Black" Stroke="Silver">
            </Rectangle>
            <Rectangle Grid.Row="5" Height="28" Width="28" Fill="Black" Stroke="Silver">
            </Rectangle>
        </Grid>
        <ItemsControl Grid.Column="1" ItemsSource="{Binding}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="8" Columns="8" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Rectangle Stroke="Blue" Fill="Black"  Height="10" Width="10"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
